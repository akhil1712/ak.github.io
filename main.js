function toggle(){
    var sec = document.getElementById('sec');
    var activeToggle = document.getElementById('toggle');

    sec.classList.toggle('active');
    activeToggle.classList.toggle('active');
}